% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fit_lrmsd_msa_mode}
\alias{fit_lrmsd_msa_mode}
\title{Maximum-likelihood point fit of the MSA model to a mode-axis profile}
\usage{
fit_lrmsd_msa_mode(
  spm,
  mode,
  lrmsd_obs,
  a1_range = c(0, 10),
  log2_a2_plus1_range = c(0, 13),
  init = NULL,
  grid_n = 25
)
}
\arguments{
\item{spm}{A single-point-mutation \code{spm} object from \code{\link[=generate_spm]{generate_spm()}} (energy_data +
the \code{dr2mat_mode} matrix; no \code{site_map}).}

\item{mode}{Integer vector of mode indices identifying the observations. Observations
are a \verb{(mode, lrmsd_obs)} vector pair rather than a data frame, so the columns of
your own table can be named anything. Every value must exist in the model.}

\item{lrmsd_obs}{Numeric vector of observed log structural divergences (the fit
target), the same length as \code{mode}.}

\item{a1_range}{Length-2 \verb{[min, max]} box bound for \code{a1}.}

\item{log2_a2_plus1_range}{Length-2 \verb{[min, max]} box bound for \code{log2(a2 + 1)}.}

\item{init}{Optional length-2 numeric start \code{c(a1, log2(a2+1))}. When \code{NULL}
(default), a deterministic coarse grid-max of the likelihood over the box is
used as the start (robust against a bad local start; cheap).}

\item{grid_n}{Number of points per axis for the default grid-max start (ignored
when \code{init} is supplied).}
}
\value{
A list. The top level is the \strong{point estimate}; goodness of fit is the
\code{gof} tibble (computed here, so there is no accessor to call):
\describe{
\item{a1, a2}{Point estimate of the stability (\code{a1}) and activity (\code{a2})
selection strengths, on the natural scale (the paper's \code{aS}, \code{aA}).}
\item{logLik}{Profiled Gaussian log-likelihood at the optimum.}
\item{cov}{2×2 covariance matrix on the \verb{(a1, log2(a2+1))} scale.}
\item{se_a1, se_a2}{Standard errors; \code{se_a2} via the delta method.}
\item{convergence}{\code{optim} convergence code (0 = success).}
\item{gof}{One-row tibble of fit quality: \code{D2} (deviance explained vs the flat
null, \code{1 - deviance/null_deviance}; at most \code{1} but \strong{unbounded below} --
negative means worse than a constant profile, a real signal, not an error),
\code{AIC}, \code{BIC}, and the primitives \code{logLik}, \code{deviance}, \code{null_deviance},
\code{nobs} (matched observations scored), \code{k} (\code{3}: \code{a1}, \code{a2}, profiled \code{sigma}),
\code{sigma_hat}. \code{AIC}/\code{BIC} are per-fit numbers, meaningful only compared against
another model fit at its own maximum.}
\item{call}{The matched call that produced this fit -- the fit itself is
axis-free, so this is what records which fitter made it.}
}
}
\description{
Mode counterpart of \code{\link[=fit_lrmsd_msa_site]{fit_lrmsd_msa_site()}}: maximises the profiled Gaussian
log-likelihood over \verb{(a1, a2)} by numerical optimisation, returning a point estimate
plus an asymptotic covariance from the Hessian at the optimum. Identical machinery to
the site fit -- the same axis-blind objective \code{loglik_lrmsd_msa()}, evaluated on
\code{spm$dr2mat_mode} instead of \code{spm$dr2mat_site}; the response index is the mode (no \code{site_map}
/ \code{pdb_site}).
}
\details{
The optimiser works in the same coordinates as the site fit: \code{a1} and
\code{log2(a2 + 1)} (so \code{a2 = 2^(log2(a2+1)) - 1 >= 0}), on the box \code{a1_range} ×
\code{log2_a2_plus1_range} — the coordinates in which the prior is uniform. The
returned covariance \code{cov} is on the \verb{(a1, log2(a2+1))} scale; the standard error
of \code{a2} is obtained by the delta method (\verb{da2/d(log2(a2+1)) = 2^(log2(a2+1)) * ln 2}).
}
\examples{
\dontrun{
ex   <- function(f) system.file("extdata", f, package = "msamodel")
wt   <- set_enm(bio3d::read.pdb(ex("1znb_A.pdb")), node = "ca",
                      model = "ming_wall", d_max = 10.5, frustrated = FALSE)
act  <- readr::read_csv(ex("znb_active_site.csv"))
spm  <- generate_spm(wt, pdb_site_active = act$pdb_site, seed = 1024)

# Synthetic observations: no empirical per-mode profile exists (see the file name).
obs <- readr::read_csv(ex("znb_lrmsd_obs_mode_syn.csv"))
ml  <- fit_lrmsd_msa_mode(spm, obs$mode, obs$lrmsd_obs)
c(a1 = ml$a1, a2 = ml$a2)
}
}
\seealso{
\code{\link[=fit_lrmsd_msa_site]{fit_lrmsd_msa_site()}} (the site counterpart),
\code{\link[=predict_profiles]{predict_profiles()}} (propagate the fit to a banded profile),
\code{loglik_lrmsd_msa()} (the objective).

Other fitting:
\code{\link[=fit_lrmsd_msa_site]{fit_lrmsd_msa_site()}}
}
\concept{fitting}
