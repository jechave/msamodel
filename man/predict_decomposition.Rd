% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{predict_decomposition}
\alias{predict_decomposition}
\title{Divergence decomposition with error bands from an ML fit (both axes)}
\usage{
predict_decomposition(fit, spm, which = c("lrmsd", "nlrmsd"))
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_msa_site]{fit_lrmsd_msa_site()}} (site) or \code{\link[=fit_lrmsd_msa_mode]{fit_lrmsd_msa_mode()}} (mode),
carrying \code{a1}, \code{a2}, \code{cov}.}

\item{spm}{The \code{spm} object from \code{\link[=generate_spm_data]{generate_spm_data()}} (the same one used for the fit).}

\item{which}{Must be \code{"nlrmsd"} (default). \code{"lrmsd"} is accepted by the signature but
errors -- the uncentred component band is not yet derived.}
}
\value{
A list with two tibbles (\verb{$site}, \verb{$mode}). Each holds the axis key (\code{site},
\code{pdb_site} for site; \code{mode} for mode), the four nested-model columns
(\code{nlrmsd_mm}...) and the three contribution columns (\verb{nphi_*}), each with its \verb{_se}.
}
\description{
\code{\link[=calculate_decomposition]{calculate_decomposition()}} with uncertainty: the nested-model profiles and the three
contributions, each with a \verb{_se} sibling, on \strong{both} response axes, from a fit. Only
\code{which = "nlrmsd"} (the mean-centred family the fit is on) is available; the uncentred
(\code{which = "lrmsd"}) band is not yet derived and errors.
}
\details{
Band construction mirrors the per-family \verb{predict_*_ml} functions:
\itemize{
\item \strong{Nested models} -- the parameter arm is differentiated at the fit's point for all
four variants (only the selected column differs), while the SPM arm is evaluated at
each variant's own \verb{(a1, a2)} (MM \verb{(0,0)}, MS \verb{(a1,0)}, MA \verb{(0,a2)}, MSA \verb{(a1,a2)}).
MM has a zero parameter band but a nonzero SPM band.
\item \strong{Contributions} -- the SPM arm is the differenced-contribution variance across the
nested models on the same scan (retaining the between-model covariance), from
\code{var_spm_nphi()} at the fit point; the parameter arm is per contribution.
}
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, pdb_site_active = c(99,101,103,162,181,184,193,223), seed = 1024)
ml  <- fit_lrmsd_msa_site(spm, znb_profile$pdb_site, znb_profile$lrmsd_i_obs)
predict_decomposition(ml, spm, which = "nlrmsd")$site
}
}
\seealso{
\code{\link[=calculate_decomposition]{calculate_decomposition()}} (point values, no bands);
\code{\link[=predict_profiles]{predict_profiles()}} (the banded profile these contributions sum to).

Other api:
\code{\link[=calculate_decomposition]{calculate_decomposition()}},
\code{\link[=calculate_profiles]{calculate_profiles()}},
\code{\link[=predict_profiles]{predict_profiles()}}
}
\concept{api}
