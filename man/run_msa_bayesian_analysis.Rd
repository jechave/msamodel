% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msa_bayesian_workflow.R
\name{run_msa_bayesian_analysis}
\alias{run_msa_bayesian_analysis}
\title{Run the full Bayesian MSA analysis from raw mutation data to decomposition}
\usage{
run_msa_bayesian_analysis(
  spm,
  observed_data,
  n_mcmc_iter = 10000,
  n_burnin = 2000,
  a1_prior_range = c(0, 10),
  log2_a2_plus1_prior_range = c(0, 13),
  fix_a1 = NULL,
  fix_a2 = NULL
)
}
\arguments{
\item{spm}{A single-point-mutation scan, as returned by \code{\link[=generate_spm_data]{generate_spm_data()}}
(e.g. the bundled \code{znb_spm}).}

\item{observed_data}{A tibble of the observed profile, with columns \code{pdb_site}
(PDB residue number) and \code{lrmsd_i_obs} (observed log structural divergence).
Site-keyed elements of the result carry both \code{i} and \code{pdb_site}.}

\item{n_mcmc_iter}{Number of MCMC iterations to run.}

\item{n_burnin}{Number of initial iterations to discard before collecting samples.}

\item{a1_prior_range}{Length-2 \code{c(min, max)} uniform prior range for \code{a1}.}

\item{log2_a2_plus1_prior_range}{Length-2 \code{c(min, max)} uniform prior range for
\code{log2(a2 + 1)}.}

\item{fix_a1}{Optional value at which to hold \code{a1} fixed instead of sampling it.}

\item{fix_a2}{Optional value at which to hold \code{a2} fixed instead of sampling it.}
}
\value{
A named list with the inputs and every analysis stage: \code{observed_data},
\code{parameter_samples}, \code{parameter_summary}, \code{prediction_samples},
\code{prediction_summary}, \code{decomposition_samples}, and \code{decomposition_summary}.
}
\description{
One call that takes a single-point-mutation scan and an observed divergence
profile through the whole site-form analysis: it preprocesses the scan, samples
the posterior of the selection strengths \verb{(a1, a2)}, predicts the divergence
profiles, summarises both, and splits the divergence into its mutation,
stability, and activity contributions. It is a convenience wrapper over the
individual steps (\code{\link[=preprocess_spm]{preprocess_spm()}}, \code{\link[=fit_lrmsd_i_msa_mcmc]{fit_lrmsd_i_msa_mcmc()}},
\code{\link[=calculate_prediction_samples]{calculate_prediction_samples()}} and the summary/decomposition functions).
}
\examples{
\dontrun{
analysis <- run_msa_bayesian_analysis(znb_spm, znb_profile)
analysis$parameter_summary
}
}
\seealso{
the wrapped steps \code{\link[=fit_lrmsd_i_msa_mcmc]{fit_lrmsd_i_msa_mcmc()}},
\code{\link[=calculate_prediction_summary]{calculate_prediction_summary()}}, \code{\link[=calculate_decomposition_summary]{calculate_decomposition_summary()}}.

Other fitting:
\code{\link[=calculate_parameter_summary]{calculate_parameter_summary()}},
\code{\link[=calculate_prediction_samples]{calculate_prediction_samples()}},
\code{\link[=calculate_prediction_summary]{calculate_prediction_summary()}},
\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}},
\code{\link[=fit_lrmsd_i_msa_mcmc]{fit_lrmsd_i_msa_mcmc()}},
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}},
\code{\link[=fit_lrmsd_n_msa_ml]{fit_lrmsd_n_msa_ml()}}
}
\concept{fitting}
