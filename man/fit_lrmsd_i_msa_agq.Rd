% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fit_lrmsd_i_msa_agq}
\alias{fit_lrmsd_i_msa_agq}
\title{Posterior fit of the lrmsd_i MSA model by adaptive Gauss-Hermite quadrature}
\usage{
fit_lrmsd_i_msa_agq(
  spm_pp,
  observed_data,
  n_nodes = 7,
  a1_range = c(0, 10),
  log2_a2_plus1_range = c(0, 13),
  init = NULL,
  grid_n = 25
)
}
\arguments{
\item{spm_pp}{Preprocessed data from \code{\link[=preprocess_spm]{preprocess_spm()}} (must include \code{site_map}).}

\item{observed_data}{Tibble with columns \code{pdb_site} and \code{lrmsd_i_obs} (the fit
target), as documented for \code{calculate_loglik_lrmsd_i_msa()}.}

\item{n_nodes}{Number of Gauss-Hermite nodes per axis (the quadrature uses an
\code{n_nodes} x \code{n_nodes} tensor grid). Default 7 (49 evaluations); the posterior
moments are already accurate at 5, the extra nodes give the credible band margin.}

\item{a1_range, log2_a2_plus1_range, init, grid_n}{Passed to \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}
to obtain the Laplace reference (box bounds, optional start, grid-max start size).}
}
\value{
A list of class \code{"msa_agq"} with the posterior summary and the quadrature:
\describe{
\item{a1, a2}{Posterior means of the stability (\code{a1}) and activity (\code{a2})
selection strengths, natural scale (the paper's \code{aS}, \code{aA}).}
\item{sd_a1, sd_a2}{Posterior standard deviations, natural scale.}
\item{ci_a1, ci_a2}{Length-2 95\% credible intervals, natural scale.}
\item{nodes}{Tibble \verb{a1, a2, log_weight} -- the quadrature nodes on the natural
scale and their unnormalized log posterior masses. The propagation primitive.}
\item{n_nodes}{Nodes per axis.}
\item{laplace}{\code{list(mu, cov)}, the Gaussian reference used to place nodes, on
the \verb{(a1, log2(a2 + 1))} scale (\code{cov} carries matching \code{dimnames}).}
\item{log_evidence}{Log marginal likelihood from the quadrature.}
}
}
\description{
The Bayesian arm of the MSA fit: it approximates the posterior of the selection
strengths \verb{(a1, a2)} by adaptive Gauss-Hermite quadrature, under a uniform prior
in \verb{(a1, log2(a2 + 1))} and the profiled Gaussian likelihood
(\code{calculate_loglik_lrmsd_i_msa()}). The quadrature is \emph{referenced} to the Laplace
(Gaussian) approximation that \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} already computes: nodes are
placed at that Gaussian's optimal locations, so a handful of likelihood
evaluations reproduce the posterior. For the bundled \code{znb_profile} the posterior
is near-Gaussian in \verb{(a1, log2(a2 + 1))}, and even a 5x5 grid (25 evaluations)
matches a dense reference posterior essentially exactly. The fit is fully
deterministic -- no seed, burn-in, or autocorrelation.
}
\details{
The quadrature is built on the unconstrained scale \code{t = log2(a2 + 1)} (where the
prior is flat and the posterior is near-Gaussian) and results are reported on the
natural scale \code{a2}, transformed at the boundary -- the standard
constrained/unconstrained convention. Node weights are stored as \strong{unnormalized
log masses} (\code{log_weight}); a probability mass is reparameterization-invariant,
so the node table is reported entirely on the natural \verb{(a1, a2)} scale and any
posterior expectation is the plain weighted sum
\code{sum(exp(log_weight) * g(a1, a2)) / sum(exp(log_weight))}. The only quantity kept
on the \code{t} scale is the Laplace covariance (see \code{laplace$cov}).
}
\examples{
\dontrun{
pp  <- preprocess_spm(znb_spm)
agq <- fit_lrmsd_i_msa_agq(pp, znb_profile)
c(a1 = agq$a1, a2 = agq$a2)
}
}
\seealso{
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (the Laplace reference / point-estimate arm),
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}} (propagate the posterior to a banded profile).

Other fitting:
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}},
\code{\link[=fit_lrmsd_n_msa_ml]{fit_lrmsd_n_msa_ml()}}
}
\concept{fitting}
