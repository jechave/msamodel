% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{weights_jm_spm}
\alias{weights_jm_spm}
\title{SPM-ensemble averaging weights over mutants}
\usage{
weights_jm_spm(spm, a1, a2)
}
\arguments{
\item{spm}{A single-point-mutation \code{spm} object, the output of
\code{\link[=generate_spm_data]{generate_spm_data()}} (only its \code{energy_data} is used).}

\item{a1}{Stability selection strength (non-negative).}

\item{a2}{Activity selection strength (non-negative).}
}
\value{
A numeric vector of averaging weights over mutants, one per row of
\code{spm$energy_data}, summing to one.
}
\description{
Turns the per-mutant fixation probabilities of the MSA model (\code{\link[=pfix_msa]{pfix_msa()}}) into
the averaging weights of the single-point-mutation (SPM) ensemble: \code{weights_jm = pfix_jm / sum(pfix_jm)}, one weight per mutant \verb{(j, m)}, summing to one. The
forward map (\code{\link[=calculate_profiles]{calculate_profiles()}}) averages its per-mutant divergences, on both
response axes, with exactly these weights.
}
\details{
The normalisation is a property of \emph{this ensemble}, not of the model: it is the
SPM scan's estimator of the mutant distribution. A different ensemble (for
example one generated by simulating a star tree over several mutations) would turn
the same \code{\link[=pfix_msa]{pfix_msa()}} probabilities into different weights. Keeping this step in
its own function marks that ensemble-specific seam explicitly.

The weights depend on \verb{(a1, a2)} and so must be recomputed whenever the selection
strengths change; they cannot be precomputed once from the scan.
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, seed = 1024)
w <- weights_jm_spm(spm, a1 = 1, a2 = 1)
sum(w)  # 1
}
}
\seealso{
\code{\link[=pfix_msa]{pfix_msa()}} (the unnormalised model probabilities);
\code{\link[=calculate_profiles]{calculate_profiles()}} (averages with these weights).

Other spm:
\code{\link[=generate_spm_data]{generate_spm_data()}}
}
\concept{spm}
