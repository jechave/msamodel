% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_profiles}
\alias{predict_profiles}
\title{Predicted divergence profiles with standard errors from an ML fit (both axes)}
\usage{
predict_profiles(fit, spm, metric = c("lrmsd", "nlrmsd"))
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_msa_site]{fit_lrmsd_msa_site()}} (site) or \code{\link[=fit_lrmsd_msa_mode]{fit_lrmsd_msa_mode()}} (mode),
carrying \code{a1}, \code{a2}, and the 2x2 \code{cov} on the \verb{(a1, log2(a2+1))} scale. One fit
drives both axes.}

\item{spm}{The \code{spm} object from \code{\link[=generate_spm]{generate_spm()}} (the same one used for the fit).}

\item{metric}{\code{"lrmsd"} (absolute) or \code{"nlrmsd"} (mean-centred). Default \code{"lrmsd"}.}
}
\value{
A list with two tibbles. \verb{$site}: \code{site}, \code{pdb_site}, the profile column
(\code{lrmsd_msa} or \code{nlrmsd_msa}), and its \verb{_se}. \verb{$mode}: \code{mode}, the same profile
column, and its \verb{_se}.
}
\description{
\code{\link[=calculate_profiles]{calculate_profiles()}} with uncertainty: the model's per-response log
structural-divergence profile evaluated at a fit's \verb{(a1, a2)}, on \strong{both} response
axes, with the value column followed by its \verb{_se} sibling. \code{metric} selects
\code{"lrmsd"} (the absolute profile) or \code{"nlrmsd"} (the mean-centred profile the fit is on).
}
\details{
The standard error sums two independent sources: the fit's parameter uncertainty
(propagated by the delta method) and the SPM finite-mutation sampling error of the
scan itself.

For \code{metric = "nlrmsd"} the profile is centred by its own mean over the full model
support. Prediction centres over all model residues, agnostic to which residues a
given dataset observes; when overlaying observed data, centre it on its own matched
support.
}
\examples{
\dontrun{
ex  <- function(f) system.file("extdata", f, package = "msamodel")
wt  <- penm::set_enm(bio3d::read.pdb(ex("1znb_A.pdb")), node = "ca",
                            model = "ming_wall", d_max = 10.5, frustrated = FALSE)
act <- readr::read_csv(ex("znb_active_site.csv"))
spm <- generate_spm(wt, pdb_site_active = act$pdb_site, ensemble = 1L)

obs <- readr::read_csv(ex("znb_lrmsd_obs_site.csv"))
ml  <- fit_lrmsd_msa_site(spm, obs$pdb_site, obs$lrmsd_obs)
predict_profiles(ml, spm, metric = "nlrmsd")$site
}
}
\seealso{
\code{\link[=calculate_profiles]{calculate_profiles()}} (point values at a given \verb{(a1, a2)}, no fit);
\code{\link[=predict_decomposition]{predict_decomposition()}} (the profile split into contributions, with standard errors).

Other api:
\code{\link[=calculate_decomposition]{calculate_decomposition()}},
\code{\link[=calculate_profiles]{calculate_profiles()}},
\code{\link[=predict_decomposition]{predict_decomposition()}}
}
\concept{api}
