% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{predict_profiles}
\alias{predict_profiles}
\title{Predicted divergence profiles with error bands from an ML fit (both axes)}
\usage{
predict_profiles(fit, spm, which = c("lrmsd", "nlrmsd"))
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (site) or \code{\link[=fit_lrmsd_n_msa_ml]{fit_lrmsd_n_msa_ml()}} (mode),
carrying \code{a1}, \code{a2}, and the 2x2 \code{cov} on the \verb{(a1, log2(a2+1))} scale. One fit
drives both axes.}

\item{spm}{The \code{spm} object from \code{\link[=generate_spm_data]{generate_spm_data()}} (the same one used for the fit).}

\item{which}{\code{"lrmsd"} (absolute) or \code{"nlrmsd"} (mean-centred). Default \code{"lrmsd"}.}
}
\value{
A list with two tibbles. \verb{$site}: \code{i}, \code{pdb_site}, the profile column, and its
\verb{_se}. \verb{$mode}: \code{n}, the profile column, and its \verb{_se}.
}
\description{
\code{\link[=calculate_profiles]{calculate_profiles()}} with uncertainty: propagates a fit's \verb{(a1, a2)} uncertainty
(delta method) and the SPM finite-mutation sampling error to the predicted profile,
on \strong{both} response axes. \code{which} selects \code{"lrmsd"} or \code{"nlrmsd"}. Each value column
gets an adjacent \verb{_se} sibling holding the standard error (parameter and SPM arms
summed under independence).
}
\details{
For \code{which = "nlrmsd"} the profile is centred by its own mean over the full model
support, and the parameter arm uses the column-centred gradient (the mean is itself a
function of the parameters); see \code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}} for the support note.
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, pdb_site_active = c(99,101,103,162,181,184,193,223), seed = 1024)
ml  <- fit_lrmsd_i_msa_ml(spm, znb_profile)
predict_profiles(ml, spm, which = "nlrmsd")$site
}
}
\seealso{
\code{\link[=calculate_profiles]{calculate_profiles()}} (point values, no bands);
\code{\link[=predict_decomposition]{predict_decomposition()}} (the profile split into contributions, with bands).

Other api:
\code{\link[=calculate_decomposition]{calculate_decomposition()}},
\code{\link[=calculate_profiles]{calculate_profiles()}},
\code{\link[=predict_decomposition]{predict_decomposition()}}
}
\concept{api}
