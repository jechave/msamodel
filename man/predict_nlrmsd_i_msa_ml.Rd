% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_nlrmsd_i_msa_ml}
\alias{predict_nlrmsd_i_msa_ml}
\title{Predicted mean-centred nlrmsd_i profile with delta-method error bands from an ML fit}
\usage{
predict_nlrmsd_i_msa_ml(
  fit,
  spm,
  level = 0.95,
  uncertainty = c("both", "spm", "parameter", "none")
)
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (carrying \code{a1}, \code{a2}, and the 2x2
\code{cov} on the \verb{(a1, log2(a2+1))} scale).}

\item{spm}{The \code{spm} object from \code{\link[=generate_spm_data]{generate_spm_data()}} (the same one used for the fit).}

\item{level}{Confidence-band coverage (default 0.95).}

\item{uncertainty}{Which error sources enter the band: \code{"both"} (default), \code{"spm"},
\code{"parameter"}, or \code{"none"}. See \code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}}.}
}
\value{
A tibble with one row per model residue: \code{i}, \code{pdb_site}, and the centred
profile summary \code{nlrmsd_i_msa_mean}, \code{nlrmsd_i_msa_lower}, \code{nlrmsd_i_msa_upper}.
}
\description{
Mean-centred counterpart of \code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}}: the per-site profile
centred by its own mean over the \strong{full model support} (all model residues),
\code{nlrmsd_i = lrmsd_i - mean_S(lrmsd)}, with a symmetric delta-method band. Because
\code{mean_S(lrmsd)} is itself a function of \verb{(a1, a2)}, the band uses the column-centred
gradient \code{g_i - mean_S(g)}; it is therefore \strong{narrower} than (not a vertical shift
of) the \code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}} band. This is the quantity the ML fit is on: the
likelihood centres both sides, so \code{nlrmsd} is what the fit has information about.
}
\details{
Support note: prediction centres over all model residues, deliberately agnostic to
which residues a given dataset happens to observe. The fit itself centres over the
observation-matched residues (its \code{fitted.values}), so predicted and fitted \code{nlrmsd}
sit at slightly different levels \strong{by design}. When overlaying observations, centre
them on their own matched support -- that is the comparison being made.

Like \code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}}, the band combines the \verb{(a1, a2)} parameter arm and
the SPM sampling arm (summed under independence), selected by \code{uncertainty}; the
column schema is stable across modes.
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, pdb_site_active = c(99,101,103,162,181,184,193,223), seed = 1024)
ml <- fit_lrmsd_i_msa_ml(spm, znb_profile)
head(predict_nlrmsd_i_msa_ml(ml, spm))
}
}
\seealso{
\code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}} (the uncentred counterpart);
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (produces the fit); \code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}} (the forward
map linearized).

Other prediction:
\code{\link[=gof_lrmsd_i_msa_ml]{gof_lrmsd_i_msa_ml()}},
\code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}},
\code{\link[=predict_lrmsd_i_nested_models_ml]{predict_lrmsd_i_nested_models_ml()}},
\code{\link[=predict_lrmsd_n_msa_ml]{predict_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_n_nested_models_ml]{predict_lrmsd_n_nested_models_ml()}},
\code{\link[=predict_nlrmsd_i_msa_decomposition_ml]{predict_nlrmsd_i_msa_decomposition_ml()}},
\code{\link[=predict_nlrmsd_i_nested_models_ml]{predict_nlrmsd_i_nested_models_ml()}},
\code{\link[=predict_nlrmsd_n_msa_decomposition_ml]{predict_nlrmsd_n_msa_decomposition_ml()}},
\code{\link[=predict_nlrmsd_n_msa_ml]{predict_nlrmsd_n_msa_ml()}},
\code{\link[=predict_nlrmsd_n_nested_models_ml]{predict_nlrmsd_n_nested_models_ml()}}
}
\concept{prediction}
