% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-doc.R
\docType{data}
\name{znb_profile_n}
\alias{znb_profile_n}
\title{SYNTHETIC observed structural-divergence profile (mode form) for 1znb_A}
\format{
A tibble with 678 rows (one per normal mode) and 2 columns:
\describe{
\item{n}{Integer mode index (the response-mode key; modes are not
structure-anchored, so there is no \code{pdb_site}).}
\item{lrmsd_n_obs}{Synthetic observed log structural divergence per mode.}
}
}
\source{
SYNTHETIC -- not empirical. See Details and
\code{data-raw/prepare_znb_data.R}.
}
\usage{
znb_profile_n
}
\description{
Per-mode observed divergence used as the fitting target for the mode arm
(\code{fit_lrmsd_msa_mode}), keyed by the mode index \code{n}. The fitter takes the
two columns as a \code{(mode, lrmsd_obs)} vector pair, so this is example data
rather than a required schema.
}
\details{
\strong{This data is SYNTHETIC, not empirical.} No empirical mode profile exists
yet (computing observed \code{dr2_n} from homologous structures + an alignment is
out of scope for \pkg{msamodel} -- it belongs to a future
protein-evolution-patterns package). It is a stand-in so the mode fit can be
exercised end-to-end until that package exists.

Generated by fitting the SITE model to the real \code{znb_profile} by
maximum likelihood (\code{fit_lrmsd_msa_site}) for a realistic \code{(a1, a2)},
evaluating the mode forward map at that point, and adding seeded Gaussian noise
(seed 2025, sd 0.30). See \code{data-raw/prepare_znb_data.R}.
}
\seealso{
\code{\link{fit_lrmsd_msa_mode}}, \code{\link{calculate_profiles}}

Other datasets:
\code{\link{znb_dataset}},
\code{\link{znb_pdb}},
\code{\link{znb_profile}},
\code{\link{znb_spm}},
\code{\link{znb_wt}}
}
\concept{datasets}
\keyword{datasets}
