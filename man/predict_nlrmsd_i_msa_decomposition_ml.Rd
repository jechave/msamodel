% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_nlrmsd_i_msa_decomposition_ml}
\alias{predict_nlrmsd_i_msa_decomposition_ml}
\title{Decomposition of nlrmsd_i_msa with delta-method bands from an ML fit}
\usage{
predict_nlrmsd_i_msa_decomposition_ml(fit, spm_pp, level = 0.95)
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (carrying \code{a1}, \code{a2}, \code{cov}).}

\item{spm_pp}{Preprocessed data from \code{\link[=preprocess_spm]{preprocess_spm()}}.}

\item{level}{Confidence-band coverage (default 0.95).}
}
\value{
A tibble with one row per residue: \code{i}, \code{pdb_site}, and a mean/lower/upper
triple for each contribution -- \verb{nphi_mut_\{mean,lower,upper\}},
\verb{nphi_stab_\{mean,lower,upper\}}, \verb{nphi_act_\{mean,lower,upper\}} (9 band columns).
}
\description{
The three contributions (\code{nphi_mut}, \code{nphi_stab}, \code{nphi_act}) of the \emph{mean-centred}
per-site profile \code{nlrmsd_i_msa}, each with a symmetric delta-method band, from a
point ML fit (\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}). Each contribution is centred by its own mean
over the full model support and banded with the column-centred gradient (see
\code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}} for the support note). The decomposition is defined only
for the centred quantity; the three \verb{*_mean} columns sum to \code{nlrmsd_i_msa} (centring
is linear), but the bands do not sum (SEs are not additive). \code{nphi_mut} (the MM
term) has a constant gradient, so its band is zero-width.
}
\examples{
\dontrun{
pp <- preprocess_spm(znb_spm)
ml <- fit_lrmsd_i_msa_ml(pp, znb_profile)
head(predict_nlrmsd_i_msa_decomposition_ml(ml, pp))
}
}
\seealso{
\code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}} (the profile it decomposes);
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (produces the fit);
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}} (the forward map).

Other prediction:
\code{\link[=gof_lrmsd_i_msa_ml]{gof_lrmsd_i_msa_ml()}},
\code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}},
\code{\link[=predict_lrmsd_i_nested_models_ml]{predict_lrmsd_i_nested_models_ml()}},
\code{\link[=predict_lrmsd_n_msa_ml]{predict_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_n_nested_models_ml]{predict_lrmsd_n_nested_models_ml()}},
\code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}},
\code{\link[=predict_nlrmsd_i_nested_models_ml]{predict_nlrmsd_i_nested_models_ml()}},
\code{\link[=predict_nlrmsd_n_msa_decomposition_ml]{predict_nlrmsd_n_msa_decomposition_ml()}},
\code{\link[=predict_nlrmsd_n_msa_ml]{predict_nlrmsd_n_msa_ml()}},
\code{\link[=predict_nlrmsd_n_nested_models_ml]{predict_nlrmsd_n_nested_models_ml()}}
}
\concept{prediction}
