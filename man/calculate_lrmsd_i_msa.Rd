% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calculate_lrmsd_i_msa}
\alias{calculate_lrmsd_i_msa}
\title{Predicted per-site log structural divergence at one selection strength}
\usage{
calculate_lrmsd_i_msa(spm_pp, a1, a2)
}
\arguments{
\item{spm_pp}{Preprocessed single-point-mutation data, the output of
\code{\link[=preprocess_spm]{preprocess_spm()}}.}

\item{a1}{Stability selection strength (non-negative). \code{0} disables stability
selection.}

\item{a2}{Activity selection strength (non-negative). \code{0} disables activity
selection.}
}
\value{
A tibble with one row per site: the site index \code{i} and the predicted
profile \code{lrmsd_i_msa}. Callers needing the PDB residue number join
\code{spm_pp$site_map} themselves (as \code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}} does).
}
\description{
The model's forward prediction in the units the fit works in: the per-site log
root-mean-square structural divergence \code{lrmsd_i = log(sqrt(dr2_i))}, at a single
pair of selection strengths \verb{(a1, a2)}. This is the sole owner of the
\code{dr2 -> lrmsd} transform -- the likelihood, the fitters, and the nested-model
expansion all obtain their predicted profile through this function rather than
re-applying \code{log(sqrt(.))} by hand.
}
\examples{
\dontrun{
pp <- preprocess_spm(znb_spm)
head(calculate_lrmsd_i_msa(pp, a1 = 1, a2 = 1))
}
}
\seealso{
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}} (the squared-divergence forward map it wraps);
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}} (the four-variant analysis expansion).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_decomposition_n_msa]{calculate_decomposition_n_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}},
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}}
}
\concept{model}
