% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{fit_lrmsd_n_msa_ml}
\alias{fit_lrmsd_n_msa_ml}
\title{Maximum-likelihood point fit of the lrmsd_n MSA model (mode form)}
\usage{
fit_lrmsd_n_msa_ml(
  spm_pp_mode,
  observed_data,
  a1_range = c(0, 10),
  log2_a2_plus1_range = c(0, 13),
  init = NULL,
  grid_n = 25
)
}
\arguments{
\item{spm_pp_mode}{Preprocessed data from \code{\link[=preprocess_spm_mode]{preprocess_spm_mode()}} (energy_data +
the \code{dr2_njm} matrix; no \code{site_map}).}

\item{observed_data}{Tibble with columns \code{n} (mode index) and \code{lrmsd_n_obs} (the
fit target), as documented for \code{calculate_loglik_lrmsd_n_msa()}.}

\item{a1_range}{Length-2 \verb{[min, max]} box bound for \code{a1}.}

\item{log2_a2_plus1_range}{Length-2 \verb{[min, max]} box bound for \code{log2(a2 + 1)}.}

\item{init}{Optional length-2 numeric start \code{c(a1, log2(a2+1))}. When \code{NULL}
(default), a deterministic coarse grid-max of the likelihood over the box is
used as the start (robust against a bad local start; cheap).}

\item{grid_n}{Number of points per axis for the default grid-max start (ignored
when \code{init} is supplied).}
}
\value{
A list with the point estimate and asymptotic uncertainty, identical in
shape to \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}:
\describe{
\item{a1, a2}{Point estimate of the stability (\code{a1}) and activity (\code{a2})
selection strengths, on the natural scale (the paper's \code{aS}, \code{aA}).}
\item{logLik}{Profiled Gaussian log-likelihood at the optimum.}
\item{deviance}{Residual deviance \code{sum(residuals^2)} (= \code{nobs * sigma_hat^2}).
A goodness-of-fit primitive; consumed by \code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}}.}
\item{null_deviance}{Deviance of the flat/mean-only null (a constant profile),
\code{sum((observed - mean(observed))^2)}. With \code{deviance}, gives
\code{D2 = 1 - deviance/null_deviance}.}
\item{nobs}{Number of matched (observed) modes the likelihood scores.}
\item{k}{Free-parameter count for AIC/BIC: \code{a1}, \code{a2}, and the profiled \code{sigma}
(\code{3}), matching the \code{logLik.lm}/\code{broom} convention.}
\item{sigma_hat}{Profiled noise scale \code{sqrt(mean(residuals^2))} at the optimum.}
\item{cov}{2×2 covariance matrix on the \verb{(a1, log2(a2+1))} scale.}
\item{se_a1, se_a2}{Standard errors; \code{se_a2} via the delta method.}
\item{convergence}{\code{optim} convergence code (0 = success).}
}
}
\description{
Mode counterpart of \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}: maximises the mode-form profiled
Gaussian log-likelihood (\code{calculate_loglik_lrmsd_n_msa()}) over \verb{(a1, a2)} by
numerical optimisation, returning a point estimate plus an asymptotic covariance
from the Hessian at the optimum. Identical machinery to the site fit; the response
index is the mode \code{n} (no \code{site_map} / \code{pdb_site}). There is no Bayesian mode
counterpart.
}
\details{
The optimiser works in the same coordinates as the site fit: \code{a1} and
\code{log2(a2 + 1)} (so \code{a2 = 2^(log2(a2+1)) - 1 >= 0}), on the box \code{a1_range} ×
\code{log2_a2_plus1_range} — the coordinates in which the prior is uniform. The
returned covariance \code{cov} is on the \verb{(a1, log2(a2+1))} scale; the standard error
of \code{a2} is obtained by the delta method (\verb{da2/d(log2(a2+1)) = 2^(log2(a2+1)) * ln 2}).
}
\examples{
\dontrun{
pp <- preprocess_spm_mode(znb_spm)
ml <- fit_lrmsd_n_msa_ml(pp, znb_profile_n)
c(a1 = ml$a1, a2 = ml$a2)
}
}
\seealso{
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (the site counterpart),
\code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}} (goodness-of-fit from this fit),
\code{calculate_loglik_lrmsd_n_msa()} (the objective).

Other fitting:
\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}},
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}
}
\concept{fitting}
