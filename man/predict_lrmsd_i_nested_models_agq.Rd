% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_lrmsd_i_nested_models_agq}
\alias{predict_lrmsd_i_nested_models_agq}
\title{Nested-model divergence profiles with posterior credible bands from an AGQ fit}
\usage{
predict_lrmsd_i_nested_models_agq(object, spm_pp, level = 0.95)
}
\arguments{
\item{object}{An \code{"msa_agq"} fit, the output of \code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}.}

\item{spm_pp}{Preprocessed data from \code{\link[=preprocess_spm]{preprocess_spm()}} (the same used for the fit).}

\item{level}{Credible-interval coverage (default 0.95).}
}
\value{
A tibble with one row per residue: \code{i}, \code{pdb_site}, and a mean/lower/upper
triple for each of the four variants -- \verb{lrmsd_i_mm_\{mean,lower,upper\}},
\verb{lrmsd_i_ms_\{mean,lower,upper\}}, \verb{lrmsd_i_ma_\{mean,lower,upper\}},
\verb{lrmsd_i_msa_\{mean,lower,upper\}} (12 band columns).
}
\description{
Propagates the \verb{(a1, a2)} posterior of an adaptive Gauss-Hermite quadrature fit
(\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}) to all four nested model variants (MM, MS, MA, MSA),
returning a posterior mean and a central credible band for each, at every residue.
It is \code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}} widened from the single full model to the four
variants: at each quadrature node the four profiles are evaluated with
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}}, then each is summarized across nodes by their
(normalized) posterior masses \code{exp(log_weight)}. All-quadrature, no Gaussian
assumption, deterministic.
}
\details{
The four variants turn the two selection strengths on or off: MM \verb{(0, 0)},
MS \verb{(a1, 0)}, MA \verb{(0, a2)}, MSA \verb{(a1, a2)}. Bands are read off the node masses as
weighted quantiles, so their tails are only as fine as the node grid; to sharpen
them, refit with a larger \code{n_nodes}.
}
\examples{
\dontrun{
pp  <- preprocess_spm(znb_spm)
agq <- fit_lrmsd_i_msa_agq(pp, znb_profile)
head(predict_lrmsd_i_nested_models_agq(agq, pp))
}
}
\seealso{
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}} (the single full-model profile);
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}} (evaluated at each node);
\code{\link[=predict_decomposition_i_msa_agq]{predict_decomposition_i_msa_agq()}} (the phi decomposition, banded).

Other prediction:
\code{\link[=predict_decomposition_i_msa_agq]{predict_decomposition_i_msa_agq()}},
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}}
}
\concept{prediction}
