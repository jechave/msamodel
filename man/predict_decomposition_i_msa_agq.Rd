% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_decomposition_i_msa_agq}
\alias{predict_decomposition_i_msa_agq}
\title{Divergence-decomposition contributions with credible bands from an AGQ fit}
\usage{
predict_decomposition_i_msa_agq(object, spm_pp, level = 0.95)
}
\arguments{
\item{object}{An \code{"msa_agq"} fit, the output of \code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}.}

\item{spm_pp}{Preprocessed data from \code{\link[=preprocess_spm]{preprocess_spm()}} (the same used for the fit).}

\item{level}{Credible-interval coverage (default 0.95).}
}
\value{
A tibble with one row per residue: \code{i}, \code{pdb_site}, and a mean/lower/upper
triple for each contribution -- \verb{phi_mut_\{mean,lower,upper\}},
\verb{phi_stab_\{mean,lower,upper\}}, \verb{phi_act_\{mean,lower,upper\}} (9 band columns).
}
\description{
Propagates the \verb{(a1, a2)} posterior of an adaptive Gauss-Hermite quadrature fit
(\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}) to the site-wise divergence decomposition, returning a
posterior mean and a central credible band for each of the three contributions
(\code{phi_mut}, \code{phi_stab}, \code{phi_act}), at every residue. At each quadrature node the
contributions are evaluated with \code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}}, then each is
summarized across nodes by their (normalized) posterior masses \code{exp(log_weight)}.
All-quadrature, no Gaussian assumption, deterministic -- the decomposition-band
path: credible bands on the phi contributions without any sampling.
}
\details{
Bands are read off the node masses as weighted quantiles, so their tails are only
as fine as the node grid; to sharpen them, refit with a larger \code{n_nodes}. Note the
band on a sum is not the sum of the per-contribution bands (quantiles do not add),
but the posterior \emph{means} do add: the three \verb{phi_*_mean} columns sum to the
full-model mean profile of \code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}}.
}
\examples{
\dontrun{
pp  <- preprocess_spm(znb_spm)
agq <- fit_lrmsd_i_msa_agq(pp, znb_profile)
head(predict_decomposition_i_msa_agq(agq, pp))
}
}
\seealso{
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}} (evaluated at each node);
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}} (the full-model profile the means sum to);
\code{\link[=predict_lrmsd_i_nested_models_agq]{predict_lrmsd_i_nested_models_agq()}} (the four nested variants, banded).

Other prediction:
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}},
\code{\link[=predict_lrmsd_i_nested_models_agq]{predict_lrmsd_i_nested_models_agq()}}
}
\concept{prediction}
