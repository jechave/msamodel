% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_lrmsd_i_nested_models_ml}
\alias{predict_lrmsd_i_nested_models_ml}
\title{Nested-model divergence profiles with delta-method bands from an ML fit}
\usage{
predict_lrmsd_i_nested_models_ml(
  fit,
  spm,
  level = 0.95,
  uncertainty = c("both", "spm", "parameter", "none")
)
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (carrying \code{a1}, \code{a2}, \code{cov}).}

\item{spm}{The \code{spm} object from \code{\link[=generate_spm_data]{generate_spm_data()}} (the same one used for the fit).}

\item{level}{Confidence-band coverage (default 0.95).}

\item{uncertainty}{Which error sources enter the band: \code{"both"} (default), \code{"spm"},
\code{"parameter"}, or \code{"none"}. See \code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}}.}
}
\value{
A tibble with one row per residue: \code{i}, \code{pdb_site}, and a mean/lower/upper
triple for each variant -- \verb{lrmsd_i_mm_\{mean,lower,upper\}},
\verb{lrmsd_i_ms_\{mean,lower,upper\}}, \verb{lrmsd_i_ma_\{mean,lower,upper\}},
\verb{lrmsd_i_msa_\{mean,lower,upper\}} (12 band columns).
}
\description{
The four nested variants (MM, MS, MA, MSA) of the predicted per-site \emph{uncentred}
profile, each with a symmetric delta-method band, from a point ML fit
(\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}). Each variant's band linearizes
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}} about the fit's \verb{(a1, a2)}; MM \verb{(0, 0)} does not
depend on the parameters, so its \emph{parameter-uncertainty} band is zero-width -- but its
SPM-sampling band is not, so MM has a nonzero total band whenever \code{uncertainty}
includes the SPM arm. For the mean-centred variants that compare to observations, use
\code{\link[=predict_nlrmsd_i_nested_models_ml]{predict_nlrmsd_i_nested_models_ml()}}.
}
\details{
Each variant carries its own selection strengths (MM \verb{(0,0)}, MS \verb{(a1,0)}, MA
\verb{(0,a2)}, MSA \verb{(a1,a2)}), so its SPM sampling weights -- and therefore its SPM band --
are computed at that variant's own point.
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, pdb_site_active = c(99,101,103,162,181,184,193,223), seed = 1024)
ml <- fit_lrmsd_i_msa_ml(spm, znb_profile)
head(predict_lrmsd_i_nested_models_ml(ml, spm))
}
}
\seealso{
\code{\link[=predict_nlrmsd_i_nested_models_ml]{predict_nlrmsd_i_nested_models_ml()}} (mean-centred counterpart);
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (produces the fit);
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}} (the forward map).

Other prediction:
\code{\link[=gof_lrmsd_i_msa_ml]{gof_lrmsd_i_msa_ml()}},
\code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}},
\code{\link[=predict_lrmsd_n_msa_ml]{predict_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_n_nested_models_ml]{predict_lrmsd_n_nested_models_ml()}},
\code{\link[=predict_nlrmsd_i_msa_decomposition_ml]{predict_nlrmsd_i_msa_decomposition_ml()}},
\code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}},
\code{\link[=predict_nlrmsd_i_nested_models_ml]{predict_nlrmsd_i_nested_models_ml()}},
\code{\link[=predict_nlrmsd_n_msa_decomposition_ml]{predict_nlrmsd_n_msa_decomposition_ml()}},
\code{\link[=predict_nlrmsd_n_msa_ml]{predict_nlrmsd_n_msa_ml()}},
\code{\link[=predict_nlrmsd_n_nested_models_ml]{predict_nlrmsd_n_nested_models_ml()}}
}
\concept{prediction}
