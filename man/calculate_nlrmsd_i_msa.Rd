% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calculate_nlrmsd_i_msa}
\alias{calculate_nlrmsd_i_msa}
\title{Predicted mean-centred per-site log structural divergence at one selection strength}
\usage{
calculate_nlrmsd_i_msa(spm_pp, a1, a2)
}
\arguments{
\item{spm_pp}{Preprocessed single-point-mutation data, the output of \code{\link[=preprocess_spm]{preprocess_spm()}}.}

\item{a1}{Stability selection strength (non-negative). \code{0} disables stability selection.}

\item{a2}{Activity selection strength (non-negative). \code{0} disables activity selection.}
}
\value{
A tibble with one row per site: the site index \code{i} and the mean-centred profile
\code{nlrmsd_i_msa}.
}
\description{
Mean-centred counterpart of \code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}}: the per-site profile centred by
its own mean over the \strong{full model support} (all model residues),
\code{nlrmsd_i = lrmsd_i - mean_S(lrmsd_i)}. This is the quantity the site ML fit is on -- the
likelihood centres both sides -- so it is the natural forward map for exploring a
scenario \verb{(a1, a2)} on the same scale the fit reports, without fitting. It is also the
point profile that \code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}} bands: the two agree exactly (the
predictor's \verb{_mean} column is this function's \code{nlrmsd_i_msa}).
}
\details{
Centring is over all model residues, deliberately agnostic to which residues a dataset
happens to observe. The fit instead centres over the observation-matched residues, so a
fitted profile sits at a slightly different level by design; see \code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}}.
}
\examples{
\dontrun{
pp <- preprocess_spm(znb_spm)
head(calculate_nlrmsd_i_msa(pp, a1 = 1, a2 = 1))
}
}
\seealso{
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}} (the uncentred profile it centres);
\code{\link[=predict_nlrmsd_i_msa_ml]{predict_nlrmsd_i_msa_ml()}} (the same quantity with delta-method bands from a fit);
\code{\link[=calculate_nlrmsd_n_msa]{calculate_nlrmsd_n_msa()}} (the mode-indexed counterpart).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_decomposition_n_msa]{calculate_decomposition_n_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=calculate_nlrmsd_i_msa_decomposition]{calculate_nlrmsd_i_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_i_nested_models]{calculate_nlrmsd_i_nested_models()}},
\code{\link[=calculate_nlrmsd_n_msa]{calculate_nlrmsd_n_msa()}},
\code{\link[=calculate_nlrmsd_n_msa_decomposition]{calculate_nlrmsd_n_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_n_nested_models]{calculate_nlrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}}
}
\concept{model}
