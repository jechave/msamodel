% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calculate_lrmsd_n_msa}
\alias{calculate_lrmsd_n_msa}
\title{Predicted per-mode log structural divergence at one selection strength}
\usage{
calculate_lrmsd_n_msa(spm_pp, a1, a2)
}
\arguments{
\item{spm_pp}{Preprocessed single-point-mutation data in mode form, the output
of \code{\link[=preprocess_spm_mode]{preprocess_spm_mode()}}.}

\item{a1}{Stability selection strength (non-negative). \code{0} disables stability
selection.}

\item{a2}{Activity selection strength (non-negative). \code{0} disables activity
selection.}
}
\value{
A tibble with one row per mode: the mode index \code{n} and the predicted
profile \code{lrmsd_n_msa}. Modes are not residue-anchored, so there is no
\code{pdb_site} column (unlike the site form).
}
\description{
Mode-indexed counterpart of \code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}}: the model's forward
prediction in the units the fit works in, the per-mode log root-mean-square
structural divergence \code{lrmsd_n = log(sqrt(dr2_n))}, at a single pair of selection
strengths \verb{(a1, a2)}. This is the sole owner of the mode \code{dr2 -> lrmsd} transform
-- the mode likelihood, the mode fitter, and the mode nested-model expansion all
obtain their predicted profile through this function rather than re-applying
\code{log(sqrt(.))} by hand.
}
\examples{
\dontrun{
pp <- preprocess_spm_mode(znb_spm)
head(calculate_lrmsd_n_msa(pp, a1 = 1, a2 = 1))
}
}
\seealso{
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}} (the squared-divergence forward map it wraps);
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}} (the residue-indexed counterpart);
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}} (the four-variant analysis expansion).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_decomposition_n_msa]{calculate_decomposition_n_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}},
\code{\link[=calculate_nlrmsd_i_msa_decomposition]{calculate_nlrmsd_i_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_i_nested_models]{calculate_nlrmsd_i_nested_models()}},
\code{\link[=calculate_nlrmsd_n_msa]{calculate_nlrmsd_n_msa()}},
\code{\link[=calculate_nlrmsd_n_msa_decomposition]{calculate_nlrmsd_n_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_n_nested_models]{calculate_nlrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}}
}
\concept{model}
