% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msa_decomposition.R
\name{calculate_decomposition_summary}
\alias{calculate_decomposition_summary}
\title{Summarise the divergence decomposition across posterior samples}
\usage{
calculate_decomposition_summary(decomposition_samples)
}
\arguments{
\item{decomposition_samples}{A data frame with columns \code{sample_id}, \code{i}, and the
three contribution columns \code{phi_mut}, \code{phi_stab}, \code{phi_act} (as returned by
\code{\link[=calculate_decomposition_samples]{calculate_decomposition_samples()}}); \code{pdb_site} is carried through if present.}
}
\value{
A long-format data frame with \code{i} (and \code{pdb_site} if present), a
\code{component} column naming the contribution, and the summary columns \code{mean},
\code{sd}, \code{median}, \code{lower}, and \code{upper}.
}
\description{
Reduces the per-sample contributions to a posterior summary, with one row per
site and contribution. For each contribution (\code{phi_mut}, \code{phi_stab},
\code{phi_act}) it reports the mean, standard deviation, median, and 95\% credible
interval over the samples. A \code{pdb_site} column is carried through when present.
}
\examples{
\dontrun{
analysis <- run_msa_bayesian_analysis(znb_spm, znb_profile)
summ <- calculate_decomposition_summary(analysis$decomposition_samples)
head(summ)
}
}
\seealso{
\code{\link[=calculate_decomposition_samples]{calculate_decomposition_samples()}} (produces the input);
\code{\link[=run_msa_bayesian_analysis]{run_msa_bayesian_analysis()}} (which calls this as its final step).

Other decomposition:
\code{\link[=calculate_decomposition_samples]{calculate_decomposition_samples()}},
\code{\link[=calculate_msa_decomposition]{calculate_msa_decomposition()}}
}
\concept{decomposition}
