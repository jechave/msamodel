% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calculate_decomposition_n_msa}
\alias{calculate_decomposition_n_msa}
\title{Per-mode divergence decomposition at one (a1, a2)}
\usage{
calculate_decomposition_n_msa(spm_pp, a1, a2)
}
\arguments{
\item{spm_pp}{Preprocessed single-point-mutation data in mode form, the output
of \code{\link[=preprocess_spm_mode]{preprocess_spm_mode()}}.}

\item{a1}{Stability selection strength (non-negative).}

\item{a2}{Activity selection strength (non-negative).}
}
\value{
A tibble with one row per mode: the mode index \code{n} and the three
contributions \code{phi_mut}, \code{phi_stab}, \code{phi_act}.
}
\description{
Mode-indexed counterpart of \code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}}: at a single pair
of selection strengths \verb{(a1, a2)} it splits the predicted mode-divergence profile
into its mutation, stability, and activity contributions (\code{phi_mut}, \code{phi_stab},
\code{phi_act}). It packages the two-step recipe -- evaluate the four nested model
variants with \code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}}, then apply the sequential split
\code{calculate_msa_decomposition()}. The three contributions sum exactly to the
full-model profile \code{lrmsd_n_msa}. Modes are not anchored to residues, so the
output has no \code{pdb_site} column.
}
\details{
This is the single-point (forward) form. Propagating a fitted posterior over
\verb{(a1, a2)} to phi with credible bands is done downstream by evaluating this
function across the posterior's nodes or draws.
}
\examples{
\dontrun{
pp <- preprocess_spm_mode(znb_spm)
head(calculate_decomposition_n_msa(pp, a1 = 1, a2 = 1))
}
}
\seealso{
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}} (the four variants it splits);
\code{calculate_msa_decomposition()} (the pure sequential split);
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}} (the residue-indexed counterpart).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}},
\code{\link[=calculate_nlrmsd_i_msa_decomposition]{calculate_nlrmsd_i_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_i_nested_models]{calculate_nlrmsd_i_nested_models()}},
\code{\link[=calculate_nlrmsd_n_msa]{calculate_nlrmsd_n_msa()}},
\code{\link[=calculate_nlrmsd_n_msa_decomposition]{calculate_nlrmsd_n_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_n_nested_models]{calculate_nlrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}}
}
\concept{model}
