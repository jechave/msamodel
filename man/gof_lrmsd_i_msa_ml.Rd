% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{gof_lrmsd_i_msa_ml}
\alias{gof_lrmsd_i_msa_ml}
\title{Goodness of fit of an lrmsd_i ML fit}
\usage{
gof_lrmsd_i_msa_ml(fit)
}
\arguments{
\item{fit}{A list from \code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (carrying \code{logLik}, \code{deviance},
\code{null_deviance}, \code{nobs}, \code{k}).}
}
\value{
A one-row tibble: \code{D2} (deviance explained vs the flat null,
\code{1 - deviance/null_deviance}), \code{AIC} (\code{-2*logLik + 2*k}), \code{BIC}
(\code{-2*logLik + k*log(nobs)}), and the primitives \code{logLik}, \code{deviance},
\code{null_deviance}, \code{nobs}, \code{k}.
}
\description{
Absolute goodness-of-fit summary for a maximum-likelihood site fit
(\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}}), in the style of \code{\link[broom:glance]{broom::glance()}} for a \code{glm}. Reports
the deviance-explained \code{D2} against a flat / mean-only null, together with \code{AIC}
and \code{BIC} and the primitives they derive from. Because the fit mean-centres the
profile, \code{D2 = 1 - deviance/null_deviance} is exactly the fraction of profile
variance explained, \code{1 - Var(residuals)/Var(observed)}.
}
\details{
\code{D2} is at most \code{1} but has \strong{no lower bound}: it is negative whenever the
prediction is worse than the flat null (\code{Var(residuals) > Var(observed)}), which
happens for parameter values that inflate the predicted amplitude. It is returned
unclamped -- a negative \code{D2} is a real signal that the fit is poor, not an error.

\code{AIC}/\code{BIC} here are per-fit numbers; they are only meaningful \emph{compared} against
another model fit at its own maximum. Comparing nested variants (MS/MA) requires
fitting them independently, which this package does not yet do.
}
\examples{
\dontrun{
pp <- preprocess_spm(znb_spm)
ml <- fit_lrmsd_i_msa_ml(pp, znb_profile)
gof_lrmsd_i_msa_ml(ml)
}
}
\seealso{
\code{\link[=fit_lrmsd_i_msa_ml]{fit_lrmsd_i_msa_ml()}} (produces the fit), \code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}} (mode
counterpart).

Other prediction:
\code{\link[=gof_lrmsd_n_msa_ml]{gof_lrmsd_n_msa_ml()}},
\code{\link[=predict_decomposition_i_msa_agq]{predict_decomposition_i_msa_agq()}},
\code{\link[=predict_decomposition_i_msa_ml]{predict_decomposition_i_msa_ml()}},
\code{\link[=predict_decomposition_n_msa_ml]{predict_decomposition_n_msa_ml()}},
\code{\link[=predict_lrmsd_i_msa_agq]{predict_lrmsd_i_msa_agq()}},
\code{\link[=predict_lrmsd_i_msa_ml]{predict_lrmsd_i_msa_ml()}},
\code{\link[=predict_lrmsd_i_nested_models_agq]{predict_lrmsd_i_nested_models_agq()}},
\code{\link[=predict_lrmsd_i_nested_models_ml]{predict_lrmsd_i_nested_models_ml()}},
\code{\link[=predict_lrmsd_n_msa_ml]{predict_lrmsd_n_msa_ml()}},
\code{\link[=predict_lrmsd_n_nested_models_ml]{predict_lrmsd_n_nested_models_ml()}}
}
\concept{prediction}
