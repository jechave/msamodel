% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{calculate_nlrmsd_n_msa}
\alias{calculate_nlrmsd_n_msa}
\title{Predicted mean-centred per-mode log structural divergence at one selection strength}
\usage{
calculate_nlrmsd_n_msa(spm, a1, a2)
}
\arguments{
\item{spm}{A single-point-mutation \code{spm} object from \code{\link[=generate_spm_data]{generate_spm_data()}}.}

\item{a1}{Stability selection strength (non-negative). \code{0} disables stability selection.}

\item{a2}{Activity selection strength (non-negative). \code{0} disables activity selection.}
}
\value{
A tibble with one row per mode: the mode index \code{n} and the mean-centred profile
\code{nlrmsd_n_msa}.
}
\description{
Mode-indexed counterpart of \code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}}: the per-mode profile centred by
its own mean over the full model support (all modes),
\code{nlrmsd_n = lrmsd_n - mean_S(lrmsd_n)}. This is the quantity the mode ML fit is on and the
point profile that \code{\link[=predict_nlrmsd_n_msa_ml]{predict_nlrmsd_n_msa_ml()}} bands (the two agree exactly). Modes are
not residue-anchored, so there is no \code{pdb_site} column. See \code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}} for
the support note (predict/scenario centres over all modes; the fit centres over the
observation-matched modes, so the two levels differ by design).
}
\examples{
\dontrun{
spm <- generate_spm_data(znb_wt, seed = 1024)
head(calculate_nlrmsd_n_msa(spm, a1 = 1, a2 = 1))
}
}
\seealso{
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}} (the uncentred profile it centres);
\code{\link[=predict_nlrmsd_n_msa_ml]{predict_nlrmsd_n_msa_ml()}} (the same quantity with delta-method bands from a fit);
\code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}} (the residue-indexed counterpart).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_decomposition_n_msa]{calculate_decomposition_n_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=calculate_nlrmsd_i_msa]{calculate_nlrmsd_i_msa()}},
\code{\link[=calculate_nlrmsd_i_msa_decomposition]{calculate_nlrmsd_i_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_i_nested_models]{calculate_nlrmsd_i_nested_models()}},
\code{\link[=calculate_nlrmsd_n_msa_decomposition]{calculate_nlrmsd_n_msa_decomposition()}},
\code{\link[=calculate_nlrmsd_n_nested_models]{calculate_nlrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}}
}
\concept{model}
