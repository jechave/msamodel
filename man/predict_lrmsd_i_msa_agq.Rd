% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{predict_lrmsd_i_msa_agq}
\alias{predict_lrmsd_i_msa_agq}
\title{Predicted lrmsd_i profile with posterior credible bands from an AGQ fit}
\usage{
predict_lrmsd_i_msa_agq(object, spm_pp, level = 0.95)
}
\arguments{
\item{object}{An \code{"msa_agq"} fit, the output of \code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}.}

\item{spm_pp}{Preprocessed data from \code{\link[=preprocess_spm]{preprocess_spm()}} (the same used for the fit).}

\item{level}{Credible-interval coverage (default 0.95).}
}
\value{
A tibble with one row per residue: \code{i}, \code{pdb_site}, the posterior summary
of the full-model profile (\code{lrmsd_i_msa_mean}, \code{lrmsd_i_msa_lower},
\code{lrmsd_i_msa_upper}), and their mean-centred counterparts (\code{nlrmsd_i_msa_mean},
\code{nlrmsd_i_msa_lower}, \code{nlrmsd_i_msa_upper}).
}
\description{
Propagates the \verb{(a1, a2)} posterior of an adaptive Gauss-Hermite quadrature fit
(\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}}) to the predicted per-site divergence profile, returning
a posterior mean and a central credible interval at every residue. For each
quadrature node the full-model profile \code{lrmsd_i_msa} is evaluated with
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}}; the per-site posterior is then those node
values weighted by their (normalized) posterior masses \code{exp(log_weight)}.
Everything -- mean and band -- comes from the same quadrature, making no Gaussian
assumption (consistent with the fit's parameter summaries). Deterministic.
}
\details{
The band is read off the node masses as weighted quantiles, so its tails are only
as fine as the node grid. To sharpen the band (especially at high \code{level}, e.g.
0.99), refit \code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}} with a larger \code{n_nodes}; on the bundled
\code{znb_profile} the band is already stable at the default.
}
\examples{
\dontrun{
pp  <- preprocess_spm(znb_spm)
agq <- fit_lrmsd_i_msa_agq(pp, znb_profile)
head(predict_lrmsd_i_msa_agq(agq, pp))
}
}
\seealso{
\code{\link[=fit_lrmsd_i_msa_agq]{fit_lrmsd_i_msa_agq()}} (produces the posterior);
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}} (evaluated at each node).

Other model:
\code{\link[=calculate_decomposition_i_msa]{calculate_decomposition_i_msa()}},
\code{\link[=calculate_decomposition_n_msa]{calculate_decomposition_n_msa()}},
\code{\link[=calculate_dr2_i_msa]{calculate_dr2_i_msa()}},
\code{\link[=calculate_dr2_n_msa]{calculate_dr2_n_msa()}},
\code{\link[=calculate_lrmsd_i_msa]{calculate_lrmsd_i_msa()}},
\code{\link[=calculate_lrmsd_i_nested_models]{calculate_lrmsd_i_nested_models()}},
\code{\link[=calculate_lrmsd_n_msa]{calculate_lrmsd_n_msa()}},
\code{\link[=calculate_lrmsd_n_nested_models]{calculate_lrmsd_n_nested_models()}},
\code{\link[=pfix_msa]{pfix_msa()}}
}
\concept{model}
